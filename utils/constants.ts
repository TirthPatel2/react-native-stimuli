export const colorOptions = ["blue", "red", "green", "grey", "orange", "yellow", "purple", "violet", "aqua"];

export const numberList = (length: number) => Array.from({ length }, (_, index) => index);
